# Known-safe findings, allowlisted by fingerprint (not by disabling the rule).
#
# 1118ec45abccfdd0e8e36d827171c96b11178df5:lib/__adversarial_secret.ts:generic-api-key:4
#   Deliberately-planted fake secret from the Phase 0 adversarial pass (proving the
#   gitleaks CI gate actually blocks a build). Random value, never a real credential for
#   any service, file removed in the very next commit. Still present in git history —
#   which is exactly what this scanner is supposed to catch — so it's allowlisted here
#   by exact fingerprint rather than removed from history. See PROGRESS.md Phase 0 entry.
1118ec45abccfdd0e8e36d827171c96b11178df5:lib/__adversarial_secret.ts:generic-api-key:4
