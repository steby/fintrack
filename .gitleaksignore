# Known-safe findings, allowlisted by fingerprint (not by disabling the rule).
#
# 91399c76e0e49505d8ca02989b9dec8fdabd8f2e:lib/__adversarial_secret.ts:generic-api-key:4
#   Deliberately-planted fake secret from the Phase 0 adversarial pass (proving the
#   gitleaks CI gate actually blocks a build). Random value, never a real credential for
#   any service, file removed in the very next commit. Still present in git history —
#   which is exactly what this scanner is supposed to catch — so it's allowlisted here
#   by exact fingerprint rather than removed from history. See PROGRESS.md Phase 0 entry.
#   Fingerprint-pinned, not path-based, so it breaks on every history rewrite — this is
#   the SECOND time that's happened (first: scrubbing seed.ts's real data before going
#   public; second: this pass, scrubbing lib/db/clean-legacy-data.ts's real data). A
#   path-based .gitleaks.toml allowlist would survive rewrites permanently instead of
#   needing a manual fingerprint update each time, but requires CI to pass an explicit
#   --config flag (gitleaks does not auto-load a repo's own .gitleaks.toml, by design —
#   auto-loading would let a malicious PR silently allowlist its own leaked secret) and
#   the exact TOML allowlist schema depends on whichever gitleaks version CI's
#   `:latest` Docker tag resolves to at the time, which can't be verified without a
#   local gitleaks binary (not installed in this environment). Deferred again for that
#   reason — real improvement, still not urgent, current fix still works. All prior
#   fingerprints for this same finding are kept below (harmless) in case a stale
#   checkout ever resolves to an older SHA.
91399c76e0e49505d8ca02989b9dec8fdabd8f2e:lib/__adversarial_secret.ts:generic-api-key:4
1118ec45abccfdd0e8e36d827171c96b11178df5:lib/__adversarial_secret.ts:generic-api-key:4
6f7574f88d3e1e139b04f0e117882df2d6c7adf1:lib/__adversarial_secret.ts:generic-api-key:4
