// Phase 1 populates the real domain schema (households, users, sessions, categories,
// bank_accounts, recurring_schedule, monthly_entries, goals — see spec.md Data Model).
// Empty for Phase 0 — this file's only job right now is to prove the migration pipeline
// (generate/apply/connect) works end to end before any feature code exists.
export {};
